// Floating controls logic is handled directly inside each layout in App.jsx
// This file exists only so old imports don't break
export default function FloatingControls({ isFullscreen }) {
  return null;
}